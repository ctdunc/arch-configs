bmf="$HOME/.global-resources/web-bookmarks"
sel=$(awk '{print $1}' ${bmf} | dmenu -p "open:" -fn "Fantasque Sans Mono-16")|| exit 
uri=$(grep ${sel} ${bmf} |awk '{print $2}')

if [ "$uri" = "" ]; then
	if [ "$sel" = "" ]; then
		exit 1
	else
		firefox -search "$sel"	
	fi
else
	firefox "$uri" 
fi
